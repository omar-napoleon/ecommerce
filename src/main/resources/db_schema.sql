CREATE TABLE BRANDS
(
    ID   INT AUTO_INCREMENT PRIMARY KEY,
    NAME VARCHAR(50) NOT NULL
);

INSERT INTO BRANDS (NAME)
VALUES ('ZARA'),
       ('H&M'),
       ('BERSHKA');

CREATE TABLE BASE_PRICES
(
    ID    INT AUTO_INCREMENT PRIMARY KEY,
    PRICE DECIMAL(10, 2) NOT NULL,
    CURR  VARCHAR(3)     NOT NULL
);

INSERT INTO BASE_PRICES (PRICE, CURR)
VALUES (30.00, 'EUR'),
       (20.00, 'EUR'),
       (20.00, 'EUR'),
       (30.00, 'EUR'),
       (30.00, 'EUR');

CREATE TABLE PRICES
(
    ID         INT AUTO_INCREMENT PRIMARY KEY,
    BRAND_ID   INT            NOT NULL,
    START_DATE VARCHAR(19)    NOT NULL,
    END_DATE   VARCHAR(19)    NOT NULL,
    PRICE_LIST INT            NOT NULL,
    PRODUCT_ID INT            NOT NULL,
    PRIORITY   INT            NOT NULL,
    PRICE      DECIMAL(10, 2) NOT NULL,
    CURR       VARCHAR(3)     NOT NULL,
    FOREIGN KEY (BRAND_ID) REFERENCES BRANDS (ID),
    FOREIGN KEY (PRICE_LIST) REFERENCES BASE_PRICES (ID)
);
--
INSERT INTO PRICES (BRAND_ID, START_DATE, END_DATE, PRODUCT_ID, PRIORITY, PRICE, CURR, PRICE_LIST)
VALUES (1, '2020-06-14-00.00.00', '2020-12-31-23.59.59', 35455, 0, 35.50, 'EUR', 1),
       (1, '2020-06-14-15.00.00', '2020-06-14-18.30.00', 35455, 1, 25.45, 'EUR', 2),
       (1, '2020-06-15-00.00.00', '2020-06-15-11.00.00', 35455, 1, 30.50, 'EUR', 3),
       (1, '2020-06-15-16.00.00', '2020-12-31-23.59.59', 35455, 1, 38.95, 'EUR', 4);
